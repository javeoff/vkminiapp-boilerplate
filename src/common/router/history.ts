import { THistoryList } from '@common/router/types/THistoryList';
import { Pages } from '@main/features/App/enums/Pages';

export const history: THistoryList = [{ type: 'panel', name: Pages.INDEX }];
